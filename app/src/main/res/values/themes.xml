<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="Theme.Cukcuk" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowSplashScreenBackground" tools:ignore="NewApi">@android:color/white</item>

        <!-- Không hiển thị logo -->
        <item name="android:windowSplashScreenAnimatedIcon" tools:ignore="NewApi">@null</item>

        <!-- Không dùng icon mặc định -->
        <item name="android:windowSplashScreenIconBackgroundColor" tools:ignore="NewApi">@null</item>
    </style>

    <style name="CustomDatePickerDialogTheme" parent="android:ThemeOverlay.Material.Dialog">
        <item name="android:colorAccent">@color/main_color</item>
        <item name="android:headerBackground">@color/main_color</item>
        <item name="android:textColorPrimary">@android:color/black</item>
        <item name="android:textColorSecondary">@android:color/black</item>
        <item name="android:datePickerDialogTheme">@style/CustomDatePickerDialogTheme</item>
        <item name="android:colorPrimary">@color/main_color</item>
    </style>

</resources>